class Solution {
    public void swapElements(int[] arr) {
        // Code here
        for(int i=0;i<arr.length-2;i++){
           // arr[i+2]=arr[i]; causes out of bounds
           
           int temp=arr[i];
           arr[i]=arr[i+2];
           arr[i+2]=temp;
        }
    }
}
