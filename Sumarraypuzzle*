class Solution {
    public void sumArray(List<Integer> arr) {
        int totalSum = 0;
        for (int num : arr) {
            totalSum += num;
        }
        for (int i = 0; i < arr.size(); i++) {
            arr.set(i, totalSum - arr.get(i));
        }
    }
}
